 - have the appengine context set in one place at any time
 
      gaerecords.SetAppEngineContext(context)
 
 - explore casting to specific strong types
 
  e.g.
  
      person := Person(people.Find(1))
      person.SetName("Mat")
      person.SetAge(27)
      person.DoSomeOtherTask()
  
  - decide if Record itself will be the thing that gets persisted
    in the datastore (via the Load and Save methods) or if it will
    use some interim class to free up the Load and Save methods